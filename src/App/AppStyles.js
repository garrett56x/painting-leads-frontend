import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  app: {
    minHeight: "100vh",
    color: "#3c4858",
    fontWeight: 400,
  },
}));

export default useStyles;
